# --- 左手専用：USBとHIDを復活させる ---
CONFIG_ZMK_USB=y
CONFIG_USB_DEVICE_STACK=y

# キーボードとして文字を打つために必須
CONFIG_USB_DEVICE_HID=y

# Studio(シリアル)とキーボード(HID)を同居させる
CONFIG_USB_COMPOSITE_DEVICE=y
CONFIG_USB_HID_BOOT_PROTOCOL=y
