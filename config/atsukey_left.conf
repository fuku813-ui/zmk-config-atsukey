# 起動時のログを見逃さないよう、起動プロセスを数秒待機させる
# (ドキュメント推奨の大きな値に近い 5000ms = 5秒 に設定します)
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=5000

# --- ログ設定 (ここが重要) ---
# スニペットを使わず、直接ドライバーを叩き起こします
CONFIG_ZMK_USB_LOGGING=y
CONFIG_LOG=y
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y
